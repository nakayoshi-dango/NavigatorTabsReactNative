import { Text, View } from 'react-native';
import FirstComponent from '../FirstComponent';

export default function ListScreen() {
    return (
        <View>
            <FirstComponent text="Listado" />
            
        </View>
    );
}