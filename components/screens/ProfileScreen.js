import { Text, View } from 'react-native';
import FirstComponent from '../FirstComponent';

export default function ProfileScreen() {
    return (
        <View>
            <FirstComponent text="Perfil" />
        </View>
    );
}